#include <stdio.h>

int main () {

    int i = 1 ;

    printf ("%d \n" , ++i) ; // ++i Means Increase, Then Use
    printf ("%d \n" , i ) ;

    return 0 ;
}

// Same We Can Do --i ; Which Means First Do (i-1) Then Use i